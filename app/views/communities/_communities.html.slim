/ req param: community
- if defined? communities
  .container
    .col-lg-6.col-lg-offset-3.col-sm-12
      .row
        h3 = region.title
        hr.hr-large.blue
        - communities.each do |c|
          .row
            h3 = c.name
            hr.hr-large.blue
          .row
            .col-lg-12
              .row  
                - c.users.limit(3).each do |u|
                  .col-md-3
                    .ch-item-community style= "background-image: url(#{u.avatar_url(220)})"
                      .ch-info
                        .info
                          - if !u.title.blank?
                            = link_to u do
                                = u.title
                    p.center.blue style= "word-wrap: break-word;"
                      = "#{u.fname} #{u.name}"
                  .col-md-3
                    = link_to community_path(c)     
                      .ch-item-community style= "background-color: #00C389"
                        p = "See the community"

